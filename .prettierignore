node_modules
dist
.tsup
coverage
.changeset
pnpm-lock.yaml
*.md.snap

# Cycle-kit tooling (synced from an external kit; formatting it here would diverge it
# from source and churn locked golden-rule files) + generated audit trail + tool scratch.
.claude
knowledge-base
architect-output
agents

# Generated by pytest when the kit's python gates run in-tree. Not ours to format, and
# it is not committed either — but `prettier --check .` walks it and fails the job.
.pytest_cache
